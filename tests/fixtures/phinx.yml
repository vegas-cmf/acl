paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: tests

    tests:
        adapter: mysql
        host: localhost
        name: vegas_test
        user: root
        pass: 'root'
        port: 3306
        charset: utf8
